const profileMessage = document.getElementById("profile-message");
if (!isLoggedIn()) location.href = "login.html";
function loadProfile() { fetch("../../server/profile.php", { headers: { Authorization: "Bearer " + getToken() } }).then(function (r) { return r.json(); }).then(function (response) { if (!response.success) throw new Error(response.error || "Could not load profile"); document.getElementById("profile-username").value = response.data.username; document.getElementById("profile-email").value = response.data.email; document.getElementById("profile-region").value = response.data.region; }).catch(function (error) { profileMessage.textContent = error.message; profileMessage.className = "form-message form-error"; }); }
document.getElementById("profile-form").addEventListener("submit", function (event) { event.preventDefault(); postForm("../../server/profile.php", { region: document.getElementById("profile-region").value }).then(function (response) { if (!response.success) throw new Error(response.error || "Could not update profile"); profileMessage.textContent = "District saved."; profileMessage.className = "form-message form-success"; }).catch(function (error) { profileMessage.textContent = error.message; profileMessage.className = "form-message form-error"; }); });
loadProfile();
